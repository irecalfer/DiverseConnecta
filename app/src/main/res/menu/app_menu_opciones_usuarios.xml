<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_editar_usuario"
        android:icon="@drawable/editar"
        android:title="@string/editarUsuario"
        android:iconTint="@color/md_theme_white"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_eliminar_usuario"
        android:icon="@drawable/quitarpersona"
        android:title="@string/eliminarUsuario"
        android:iconTint="@color/md_theme_white"
        app:showAsAction="ifRoom" />
</menu>